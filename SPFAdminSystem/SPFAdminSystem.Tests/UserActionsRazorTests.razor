@inherits TestContext

These tests are written entirely in razor and C# syntax.

Learn more at https://bunit.dev/docs/getting-started/writing-tests.html#creating-basic-tests-in-razor-files

@code {
    [Fact]
    public void ThereAreXUserActions()
    {
        // Arrange
        var cut = Render(@<SPFAdminSystem.Pages.DatabasePages.UserActions />);
        var tableCells = cut.FindAll("td");
        // Assert that content of the paragraph shows counter at zero
        Assert.Equal(tableCells.Count, 4);
    }
}
